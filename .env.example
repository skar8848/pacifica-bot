# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Pacifica
PACIFICA_NETWORK=testnet
BUILDER_CODE=Trident
BUILDER_FEE_RATE=0.0005

# Encryption key for agent wallets (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your_fernet_key_here

# Solana RPC (for SOL balance checks)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
